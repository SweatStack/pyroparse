# HTTP Server Benchmark

End-to-end FIT-to-Parquet conversion: Python direct vs local HTTP.

## Environment

| | |
|---|---|
| CPU | Apple M1 Pro |
| RAM | 32 GB |
| OS | macOS 26.3.1 |
| Python | 3.10.10 |
| PyArrow | 23.0.1 |
| Local server | uvicorn + starlette (localhost) |

## Method

For each file, the benchmark measures:

1. **Python direct** — `Activity.load_fit()` + `activity.to_parquet()` in the
   same process. No network, no server overhead. This is the baseline: the
   raw cost of FIT parsing and Parquet serialization.
2. **Local HTTP** — upload FIT file to `POST /convert` on localhost, receive
   Parquet response. Adds multipart encoding, HTTP framing, temp file I/O on
   the server, and response serialization. Network latency is negligible
   (loopback) so the overhead is purely from request handling.

## Dataset

| | |
|---|---|
| Files | 49 (random sample) |
| Total rows | 170,359 |
| Total FIT size | 10.0 MB |
| Total Parquet size | 2.8 MB |
| FIT file size | 199 &plusmn; 161 KB (min 8, median 151, max 890 KB) |

## Summary

| Operation | median | p5 | p95 | max |
|---|--:|--:|--:|--:|
| **Python direct** | **5.0 ms** | 1.7 ms | 17 ms | 31 ms |
| **Local HTTP** | **6.6 ms** | 3.0 ms | 19 ms | 33 ms |
| **Local overhead** | **1.3 ms** | 0.92 ms | 1.9 ms | 6.1 ms |


### Ratio vs Python direct

| | median | p5 | p95 |
|---|--:|--:|--:|
| Local / Python | **1.30x** | 1.09x | 1.80x |

---

*Generated by `scripts/benchmark_http.py` — 49 random files from `data/fit/`*
